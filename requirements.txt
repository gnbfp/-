# 飞书 SDK（字段与回调写法已按 1.7.3 核对过）
lark-oapi==1.7.3

# 文档解析
PyMuPDF>=1.23.0        # find_tables() 需要 1.23.0+
python-docx>=1.2.0

# 甘特图
matplotlib>=3.10.0

# LLM：DeepSeek 是 OpenAI 兼容接口，直接用 HTTP 客户端即可
httpx>=0.28.0

# 测试
pytest>=8.0

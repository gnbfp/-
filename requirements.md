# 需求真源 · 小组作业项目经理机器人

> **新来的先读 `AGENTS.md`（入口与当前状态），名词看不懂查 `docs/GLOSSARY.md`。**
> **本文件是需求的唯一真源。** 任何 AI 或协作者动手前必须先读完全文。
> **主要依据 = `docs/定稿文档-v2.md`**（用户 2026-09-11 确认的定稿文档，已逐字转录归档）。
> 需求冲突时，以 `docs/requirements/` 下的两张截图原件为准。
> 截图之外的冲突**一律听定稿文档**（《方案一.docx》与旧对话都让位），见 D-32 / §10。
> **本文件没有写到的地方，禁止臆想。** 见 §8 未定义清单，遇到就停下问人。
> 历史清理：v1（三 Agent 架构版）与旧架构稿已作废并删除，本仓库不再存在。

---

## 0. 一句话

小组在飞书群里丢一份作业书 → 机器人抽出评分点 → 拆成任务卡 → 群里投票定方向 → 私聊收志愿并自动分配 → 执行期催办 → 输出执行报告。

机器人是**"群里的第 N+1 个成员"**：智能只押在拆解（M3），聊天只是交互层；它不提供思路、不替人写作业，只主持流程、匿名代言、发产物。人只做两个动作——**私聊填志愿、群里投票**。

---

## 1. 硬性边界（不可突破，B = Boundary）

| 编号 | 边界 |
|---|---|
| B1 | 系统 = **1 个 Agent + M0–M8 九个模块**，单进程 Python 服务。**不是三 Agent**；不存在 Brain / Feishu / Data 的进程或目录划分 |
| B2 | 全项目**只有三处 LLM 调用**：M1 输入解析、M2 方向候选、M3 任务拆解。**M4–M7 是纯规则与模板，零智能** |
| B3 | M8 **不在运行时**，是离线评测工具，旁挂在主链路之外 |
| B4 | 数据存 **JSON 文件**（落盘位置统一为仓库根目录 `data/`，见 `docs/ARCHITECTURE.md` §4），**不用数据库** |
| B5 | 飞书机器人是**唯一 UI**；收消息走 **WebSocket 长连接**，不用公网回调。（2026-09-11 用户确认：**飞书侧已实测通过**，草稿机器人可运行） |
| B6 | 存在**命令行主链路**（作业书→任务卡→核对清单），与飞书群入口并行 |
| B7 | **不写飞书文档。** v1 的"周报写入飞书文档"已作废，**不需要 `docx` 文档权限** |
| B8 | **LLM 不判定自己的产出。** 判定权在代码（M3 循环内）或独立脚本（M8） |
| B9 | **拍板永远在人。** 机器兜底到建议为止 |
| B10 | 指令用**前缀精确匹配**，不做自然语言理解 |

---

## 2. 五层结构

| 层 | 模块 | 性质 |
|---|---|---|
| 交互层 | M0 机器人网关 | 身体：消息循环 + 指令路由 |
| 智能层 | M1 输入解析 / M2 方向共识 / M3 任务拆解 | 大脑：仅有的三处 LLM 调用 |
| 协作层 | M4 志愿分配 / M5 匿名代言 | 手脚：纯规则状态机 |
| 流程层 | M6 催办 | 手脚：定时扫描 |
| 交付层 | M7 执行报告 | 手脚：模板渲染 |
| （旁挂）证据层 | M8 评测集 | 离线，不进运行时 |

---

## 3. 用户场景（逐条来自截图，未加推断）

| # | 场景 | 截图来源 |
|---|---|---|
| S1 | 机器人拉入测试群，发送任意消息 → 机器人回复指令列表（5 条） | T01 / M0 |
| S2 | 群里发一份作业书 → 抽出评分点，产出 ≥3 张任务卡，每张含评分点溯源字段 | T02 / M1 / M3 |
| S3 | 输入含明确评分占比的作业书 → 每个评分点至少映射一张任务卡，覆盖率可复算 | T03 |
| S4 | 作业书含模糊要求（如"内容充实"）→ 不硬拆，标记为"需组长确认" | T04 |
| S5 | 群里发「方向」口令 → 出 2–3 个候选方向，回复数字投票，过半落定 | T05 / M2 |
| S6 | 3 人私聊填志愿且无冲突 → 按志愿直接分配，人人有任务 | T06 / M4 |
| S7 | 2 人私聊抢同一模块 → 先到先得，后到者落入第二志愿 | T07 / M4 |
| S8 | 有人不回复志愿私聊 → 兜底分配，仍保证每人至少 1 个任务 | T08 / M4 |
| S9 | 私聊发「我想提议：...」→ 群里匿名发布，不泄露姓名；系统内部有记录 | T09 / M5 |
| S10 | 模块 A 依赖模块 B → 任务卡 `depends_on` 正确，甘特图体现先后 | T10 |
| S11 | 任务临期未标记完成 → 群内 @负责人提醒真实送达（有送达记录） | T11 / M6 |
| S12 | 全流程跑完一份作业书 → 输出核对清单，覆盖率数字与脚本复算一致 | T12 / M7 |
| S13 | 发一篇非作业书文本（如新闻）→ 拒拆或要求确认，不硬拆 | T13 |

> 注：S1 / T01 里的"5 条"是截图原文。后来加的两条指令（`完成 T3` / `登记`）的由来见 §7.1、§7.7 与 D-22 / D-34。

---

## 4. 模块与优先级（原文照录）

| 功能 | 优先级 | 完成定义 |
|---|---|---|
| M0 机器人网关 | P0 | 测试群收发消息跑通，5 条指令全部路由正确 |
| M1 输入解析 | P0 | 3 份真实作业书评分点全部抽出，字段校验通过 |
| M2 方向候选（简版） | P0 | 触发口令出 2-3 个候选，回复数字投票可收敛 |
| M3 任务拆解 | P0 | 5 份作业书跑通自检循环，覆盖率达标（初始参数 80%，待校准） |
| M4 志愿私聊指令 | P0 | 分配规则全过 T06-T08 |
| M5 匿名代言 | P0 尾部 | 私聊提议 → 群里匿名发布，不泄露姓名；进度落后时第一个牺牲 |
| M7 执行报告（简版） | P0 | 命令行跑出评分点核对清单 |
| M8 评测集（前 5 份基线） | P0 | 人工拆解基线标好，覆盖率脚本可复算 |
| M6 催办 @提醒 | P1 | 群里 @负责人真实送达，两档临期提醒 |
| 投票升级交互卡片 | P1 | 从"回复数字"升级为点选卡片 |
| M7 执行报告（完整版） | P1 | 分配总表 + 甘特图成品发群 |
| 组长查询匿名提交者 / 改派 | P1 | 私聊查询口令 + 改派指令 |
| M6 全量评测（15 份） | P1 | human vs agent 对比表产出 |

> 注 1：M0 完成定义里的"5 条指令"是截图原文。后来另立两条：第 6 条 `完成 T3`（定稿 §3.7 / D-22）、第 7 条 `登记`（D-34）。见 §7.1 与 §7.7。
> 注 2：表末"M6 全量评测（15 份）"的编号有误，实为 **M8** 的扩量评测（定稿 §3.9 / D-27）。原文照录不改字，勘误在此登记。

---

## 5. 验收标准

**T01–T13 全表通过即为验收完成。** 用例原文见 `docs/requirements/README.md`。

- **验收方式 = 人工验收清单，不是自动化测试脚本**（定稿 §8「验收纪律」）：每条用例跑一次、记录一次结果，演示材料里附 **T01–T13 执行记录表**。这套做法采纳自"研采"方案的方法论（D-26）
- 用例与模块的对应（定稿各节"验收"行）：T01→M0、T02–T04→M1、T05→M2、T06–T08→M4、T09→M5、T11→M6、T12→M7
- **勘误**：定稿 §3.4（M3）那节写"验收：T06–T08、T13"，与 §3.5（M4）的用例撞车，判定为笔误；M3 由 T02 / T03 覆盖（任务卡 + 覆盖率）

---

## 6. 数据模型

### 6.0 作业元信息 assignment meta —— M1 产出

定稿 §3.2：M1 的输出除评分点清单外，还必须含**课程名、作业标题、交付形式、截止时间**。

```json
{ "course": "编译原理", "title": "C 语言课程设计",
  "submission": "源码 + 报告", "deadline": "2026-09-19T23:59",
  "source_file": "作业书.pdf" }
```
- 落盘 `data/assignment.json`：写入 M1；读取 M6（催办）、M7（分配总表 / 甘特图截止线）
- **截止时间只有作业级这一个**——一份作业一根截止线。任务卡**不设** `due_date`（D-23）；定稿 §3.4 的任务卡字段表里也确实没有这一项

### 6.1 评分点 rubric point —— M1 产出

```json
{ "id": "R1", "quote": "作业书原文片段", "weight": 40,
  "observable": "可观察的验收描述", "status": "normal" }
```
- `quote` 必须是作业书**原文**，用于防 LLM 编造评分点（D-03）
- `status` ∈ `normal`（可拆）/ `ambiguous`（模糊，如"内容充实"）（D-19）
- `weight` 是**可选**的——定稿 §3.2 原文写的是"分值**或**权重"，作业书未必标占比（如 T02 那份）
  - 缺失的点不参与均衡计算；权重软校验整体跳过（§7.5 待定义-32，已关闭）

### 6.2 任务卡 task card —— M3 产出（7 字段，定稿 §3.4）

```json
{ "task_id": "T1", "module_name": "实现登录模块",
  "rubric_refs": ["R1","R3"], "effort_hours": 6, "depends_on": ["T2"],
  "deliverable": "一个源文件", "acceptance": "从 R1 原文改写，可被核对清单复用" }
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `task_id` | string | `T1`、`T2`…… 全局唯一 |
| `module_name` | string | 模块名，**动词开头**（如"实现登录模块"） |
| `rubric_refs` | array | **必填**。溯源字段：对应评分点编号 **+ 原文引用**。演示时评委看的就是它——证明 agent 真读懂了作业书 |
| `effort_hours` | number | 工作量估计（人时）。只用于均衡计算与甘特图，**不对外承诺** |
| `depends_on` | array | 前置任务的 `task_id` 列表；空数组 = 可立即开始 |
| `deliverable` | string | 这块交什么：一个源文件 / 一段报告章节 / 一页 PPT |
| `acceptance` | string | 怎么算做完。从对应评分点原文改写，**可被核对清单复用** |

- `rubric_refs` **落盘只存 `id` 数组**（`["R1","R3"]`），**展示时才 join 出原文引用**。理由：§7.3 的 `check()` 是 id 集合运算（`eligible <= covered`），数组里塞不得原文（D-03 / D-24）
- "模块"是任务卡面向组员的通俗叫法，**数据模型里就是 `task_id`**（D-14）

### 6.3 志愿 preference（D-14）

```json
{ "user_id": "u_张三", "ranked_task_ids": ["T2","T5"], "submitted_at": "ts" }
```

### 6.4 分配 assignment（D-14 / D-20）

```json
{ "task_id": "T2", "assignee": "u_张三", "source": "volunteer_1",
  "completed_at": null }
```
- `source` ∈ `volunteer_1`（第一志愿命中）/ `volunteer_2`（落入第二志愿）/ `auto`（未填表兜底）/ `leader`（组长指定与改派，P1）
- 改派时 `source` 翻成 `leader`，**不留 history**（P1 不做审计链）
- `completed_at`：`null` = 未完成。组员私聊「`完成 T3`」（§7.1 第 6 条）把它写成时间戳。M6 靠它判"临期**未**完成"，M7 核对清单靠它画勾；**MVP 不做提交物上传**（D-31）

### 6.5 提议 proposal —— M5 产出（待定义-8 已关闭）

- 落盘 `data/proposals.json`：写入 M5；读取 M5 与**组长查询**
- **必须留痕真实身份**：群里匿名发布，但 `data/proposals.json` 里记下提交者 `user_id`（定稿 §3.6「防滥用：系统内部记录提交者，组长可查询」）
- 发布文案是"有组员提议：……"，内容**原样转达**，不修改、不总结、不加工
- 组长查询**为 P1**；查询口令名尚未拍板（见 §8.1 待定义-36）

### 6.6 花名册 roster —— M0「登记」产出（D-34）

```json
{ "leader": "u_张三",
  "members": [ { "open_id": "u_张三", "name": "张三" },
               { "open_id": "u_李四", "name": "李四" } ],
  "registered_at": "ts", "confirmed_by": "u_张三" }
```
- 落盘 `data/members.json`：写入 M0（登记流程）；读取 M2（投票分母）、M4（私聊广播对象）、M5（组长身份）、M6（@对象）、M7（分配总表上的名字）
- `leader` 有且只有一个；`members` 至少 2 人
- 同时存 `open_id`（用来私聊 / @）和 `name`（用来给人看：分配总表、甘特图）
- **重发「登记」即整体覆盖**，不做版本历史

---

## 7. 关键机制

### 7.1 指令路由表（前缀精确匹配 + 状态优先，不做自然语言理解）

| 前缀 | 目标 | 说明 |
|---|---|---|
| `作业书` + 文件 | M1 | 解析评分点，清单**发群全员可见**（群 / 私聊都行） |
| `方向` | M2 | 基于评分点生成 2–3 个候选大方向并发起投票 |
| `拆解` | M3 | 评分点 → 任务卡（**不是**"评分为"） |
| `你想做哪一块` | M4 | 志愿填报（**私聊**） |
| `我想提议：…` | M5 | 匿名代言（**私聊**） |
| `完成 T3` | M6 | 标记任务完成（**私聊**，MVP 不做提交物上传） |
| `登记` + @组员 | M0 | 建 / 更新花名册（**群聊**，两步确认，见 §7.7） |

> **关于条数**：T01 截图原文写"指令列表（5 条）"，指的是 M0 指令集那 5 条（`作业书` / `方向` / `拆解` / `你想做哪一块` / `我想提议：`）。此后又长出两条：`完成 T3`（定稿 §3.7，T11 靠它才可测 —— D-22）和 `登记`（建花名册 —— D-34）。**路由表实收 7 条**；T01 的口径如何改述，见 §8.1 待定义-37。

**「`作业书` + 文件」这一行有个待实测的坑（2026-09-12 记）**

飞书**一条消息只能是一种类型**：要么纯文字、要么纯文件，不能一条里"半文字半文件"。所以"说一句 `作业书` 同时挂上附件"在客户端很可能被**拆成两条**发出：

    [群] 张三：[附件] 课程设计报告.pdf     ← 这条没有 @ 机器人
    [群] 张三：@机器人 作业书               ← 这条 @ 了

而群消息的接收权限是 `im:message.group_at_msg:readonly`（**只收被 @ 到的群消息**）。若真是两条，第二条收得到、第一条收不到 —— 机器人知道"有人要交作业书"，手里却没有文件，**主链路断在第一环**。

**私聊不一样**：私聊里机器人不需要 @ 就收得到该用户的消息，所以"先发文件、再发 `作业书`"两条都到得了。路由表本就写着"群 / 私聊都行"，**不改需求**。

- **待实测**：客户端到底发一条还是两条 → §8.1 待定义-42
- **测法**：`python -m tools.probe_feishu`（打印每条事件的 `message_type` / `@` 个数 / `file_key` 能否下载）
- **应对**（D-42）：演示与验收走**私聊投递**；群投递作备选

未命中任何前缀 → 回复"我只会这几件事" + 指令列表（T01）。这是刻意的产品性格，不做开放式闲聊兜底。

**路由优先级：先看状态，再看前缀（B10 的补充 —— D-33）**

两条补充：**先把 @段 摘掉**，以及**裸数字要靠状态解释**。

0. **先剥 @段，再做前缀匹配**：`@小组作业机器人 拆解` 要能认成 `拆解`。但 **@ 到的人要单独留着**——每个人的 `open_id` 就在 @ 结构里，「登记」表单全靠它拿 id（见 §7.7）
1. 再查 `data/state.json` 有没有 `awaiting`（当前在等什么）：
   - `awaiting = vote`（群里正在投票）→ 纯数字 = **票号**
   - `awaiting = preference`（私聊刚问过"你想做哪一块"）→ 纯数字 = **志愿序号**
   - `awaiting = register`（群里正在走登记表单）→ 这条消息按**表单内容**解析
2. 不在等数字 / 等表单 → 走上面的前缀精确匹配（7 条）
3. 仍然命中不了 → 回复指令列表（T01）

**投票窗口（M2）**

- **开启**：M2 把候选方向发到群里的那一刻
- **分母 = 已投票的人**（D-35，用户 2026-09-11 拍板）：某个方向拿到**已投票者过半**（票数 > 已投票数 ÷ 2）即落定
- **关闭**：满足任一即关 ——
  1. 有方向拿到**已投票者过半** → 该方向落定
  2. **超时**（默认 10 分钟）
  3. **组长直接拍板**
- **窗口内的规矩**：一人一票；**后投覆盖先投**（改票不用特殊指令）；窗口一关，裸数字立刻不再被当成票
- **超时且无人过半**（例如 3 人投出 3 个不同方向）→ 机器人报"暂时没有方向过半"，**交组长拍板**，或任何人再发一次「方向」（B9：拍板永远在人）
- **⚠️ 我加的一道保险（工程默认 D-36，可推翻）**：按"已投票者过半"，**第 1 个人投完就已经"过半"了**（1 > 0.5），投票会当场结束。所以默认加一道门槛：**参与投票的人数要达到花名册人数的过半（向上取整），才允许按"已投票者过半"落定**；没到门槛就等满 10 分钟。花名册来自 §6.6。**不想要这道门槛，删掉这一句即可**（那就变成"谁先投谁定"，演示上更干脆、但一人就能带走方向）
- **已知残留风险**：群里**不强制 @机器人**，所以窗口内别人闲聊打出的纯数字会被计成票。兜底 = 窗口短 + 一人一票 + 后投覆盖；若实测误判严重，再启用 §8.1 的备选 1（@机器人才算指令）

### 7.2 覆盖率的三个数字（**分母不同、用途不同，禁止混用** —— D-19）

| 数字 | 分母 | 要求 | 谁判 |
|---|---|---|---|
| 循环退出 | 可拆评分点（`status=normal`） | **100%** | 代码 |
| D5 门 | **人工标注**的可拆评分点 | **≥80%**（初始参数） | M8 独立脚本 |
| M8 报告值 | 同 D5 分母 | 实际值，**不设阈值** | 独立脚本 |

不把循环阈值降到 80% 的理由：生成器手上拿着完整清单，"每点至少一卡"是可满足指令，循环存在的意义就是第 2 轮补漏；而降阈值等于允许生成器偷懒。D5 的 80% 量的是"对人工基线的达成度"，两码事。

### 7.3 M3 自检循环（**代码判定，不是 LLM 判定** —— D-16 / D-19）

```python
def check(cards, rubric):
    if not cards:
        return False                       # 空集 → 直接判不达标（原待定义-31）
    eligible = {r.id for r in rubric.points if r.status == "normal"}
    covered  = {ref for c in cards for ref in c.rubric_refs}
    hi = max_hours(cards)
    lo = max(min_hours(cards), 0.5)        # 工时地板 0.5，避免除零
    return eligible <= covered and hi / lo <= 3
```
- **待定义-31 已关闭**（工程默认，本轮拍板生效，D-25）：`cards` 为空 → 短路返回不达标；`min_hours` 取下限 0.5
- 流程：生成 → 最多 2 轮 `check()` → 失败详情喂回重拆 → 输出附覆盖率
- 用**子集**判定（`<=`），卡片多引用模糊点不算错
- 模糊点仍按 T04 推给组长确认；确认后 `status` 改回 `normal`，重新进分母
- **病态边界**：全部评分点都是 ambiguous → **拒拆**，走"需人工"路径（与 T13 同类）
- 2 轮后仍不达标 → 输出当前最优 + 未达标项标注（如"R4 未覆盖"）发群，由人决定（D-18）

### 7.4 均衡度两套口径（**分两个函数，禁止互相复用** —— D-17）

| 用途 | 公式 |
|---|---|
| 循环内过/不过判据 | `max/min ≤ 3` |
| M8 评测报告展示值 | 归一化极差 `1 − (max−min)/Σ` |

### 7.5 作业书抽取（D-15 / D-21）

- M1 的 LLM **只吃 `{ text: string }`**
- PDF → PyMuPDF；Word → python-docx；粘贴纯文本 → 直通
- **表格用 `find_tables()` 单独抽**（需 PyMuPDF 1.23.0+），每行序列化为"列名：值｜列名：值"，与正文用 `=== 表格区域 ===` 分隔后拼进输入
  - **表格行分隔（待定义-33 已关闭，D-25）**：列内用 `｜`，**每行前加 `- ` 前缀、行间用换行**，避免跨行粘连
- **权重加总软校验**：weight 加总明显偏离 100（<90 或 >110）→ 附警告"评分标准解析疑似丢行，请对照原文核对"。**软警告不拒收**
  - **前置条件（待定义-32 已关闭，D-25）**：仅当**全部**评分点都有 `weight`、且能归一化到百分制时才执行。有缺失或本来就不是百分制（如 20 分制）→ **整体跳过，不误报**
- **拒收两种**：扫描版 PDF（空文本层 → 回复"请发文字版"）、图片（OCR 已砍，P2）
- 双栏排版不过度投入，靠上面两道兜住主要风险

### 7.6 「方向」共识（M2）—— 待定义-6b 已关闭

- **「方向」= 作业的选题大方向**。不是章节，不是任务卡，是"这份作业我们打算做什么题目"
- 流程（定稿 §3.3）：
  1. 组员先在群里**自由讨论**，**机器人不插话**
  2. 无人提案、或讨论不收敛时，**任何人**发「方向」触发
  3. 机器人**基于评分点清单**生成 2–3 个候选大方向发到群里，明确标注"仅供参考，由全组拍板"
  4. 组员**回复数字投票**，**过半即落定**
  5. **组长已有方向时直接拍板跳过**本环节
- 边界：候选方向是从作业书评分点长出来的"脚手架"，属于"读懂作业书"的智能延伸，**不是替人思考**；机器人平时不提供任何思路
- 投票需要在多条消息之间保持状态（候选列表 + 各人票数）→ 落在 `data/state.json`，见 `docs/ARCHITECTURE.md` §4

### 7.7 「登记」建花名册（M0，两步确认 —— D-34）

群聊里，**组长** @机器人 发「登记」→ 机器人回一张**空白表单** → 组长照着填、一条消息发回来 → 机器人回显**填好的表**请他确认 → 组长回「同意」→ 落盘 `data/members.json`（§6.6）。

1. **触发**：群里发 `登记`（@不@机器人都行，见 §7.1 的剥 @段规则）
2. **机器人回空白表单**（模板，让发起人照着填）：
   ```
   请照这个样子填，@到每个人：
   登记
   组长：@某人
   组员：@某人 @某人 @某人
   ```
3. **发起人填写并发出** —— **必须用 @ 人，不要打名字**：@ 结构里自带 `open_id`，机器人因此**根本不需要"读取群成员名单"那项权限**（这正是这条设计的价值）
4. **机器人回显规范化后的表**：谁是谁、共几人（含不含组长），并附一句"回复「同意」保存，回复别的就作废"
5. **发起人回「同意」** → 写入 `data/members.json`；回别的、或超时（默认 5 分钟）没回 → 作废，**不动原有名单**
6. **重发「登记」即整体覆盖**旧名单（不做历史版本）

- 硬规则：`组长` 有且只有一个；`组员` 至少 2 人
- 组长身份在表单里**明写**（一般就是发起登记的这个人自己 @ 自己），**不再靠猜**——定稿里"组长拍板跳过方向 / 组长可查询 / 组长改派"都要用到它

---

## 8. 未定义清单 —— **遇到就停下问人，禁止臆想**

> 本清单已于 2026-09-11 清算（依据 = `docs/定稿文档-v2.md`）。**"禁止臆想"的规矩不变**，但作用范围收窄为：§8.1 那张"仍开放"表（**已无挡 P0 编码的条目**）、§8.3 里标"仍开放"的三项。

### 8.1 会直接影响编码的

**已关闭（结论已写入正文，出处写明）**

| 编号 | 原问题 | 结论 | 出处 |
|---|---|---|---|
| 待定义-6b | 「方向」指什么 | = 作业**选题大方向**；完整流程见 §7.6 | 定稿 §3.3 |
| 待定义-8 | M5 完整链路 / 内部记录存哪、谁能查 | 存 `data/proposals.json`、**留痕真实身份**、群内匿名发布；组长可查询**为 P1**（查询口令名待定，见下方开放项） | 定稿 §3.6 |
| 待定义-9 | M6 编号出现两次 | **笔误**：P1 那条"M6 全量评测（15 份）"实为 **M8** 的扩量评测。M6 只有"催办"一个含义 | 定稿 §3.9 编号 |
| 待定义-11 | M8 用途：比赛评分项 or 内部自测 | **两者都是**——既是全项目可信度来源（内部调优依据），也是"可验收成果"评分项的硬通货，对比表进演示材料 | 定稿 §3.9 |
| 待定义-12 | T01–T13 形态 | **人工验收清单**，不是自动化脚本。每条跑一次记一次结果，附执行记录表 | 定稿 §8 |
| 待定义-31 | `check()` 空集 / 除零 | 空集 → 短路判不达标；`min_hours` 地板 0.5 | §7.3 / D-25 |
| 待定义-32 | 权重软校验前置条件 | 仅当 weight 全部存在且可归一化到百分制才执行，否则整体跳过 | §7.5 / D-25 |
| 待定义-33 | 表格行分隔符 | 每行前加 `- ` 前缀、行间换行 | §7.5 / D-25 |
| 待定义-34 | 无截止时间字段、无"标记完成"指令 | 截止时间 = M1 抽出的**作业级** `deadline`（§6.0），任务卡不设 `due_date`；"完成" = 私聊「**完成 T3**」（§7.1 第 6 条） | 定稿 §3.2 / §3.7 |
| 待定义-35 | 两档临期阈值 | **48 小时 / 24 小时** | 定稿 §3.7 |
| 待定义-38 | 数字回复怎么进来 | **状态优先**：先查 `state.json` 的 `awaiting`，再走前缀匹配；投票窗口 = 过半 / 超时 10 分钟 / 组长拍板关闭，一人一票、后投覆盖。详见 §7.1 | §7.1 / D-33 |
| 待定义-39 | 组员名单 / `open_id` 从哪来 | **群内两步确认的「登记」表单**（§7.7）：组长 @人报名单，id 从 @ 结构里直接拿，**不需要"读群成员名单"权限** | §7.7 / D-34 |
| 待定义-40 | 机器人怎么知道谁是组长 | 登记表单里**明写** `组长：@某人`，落在 `data/members.json` 的 `leader` | §7.7 / D-34 |
| 待定义-41 | 群投票"过半"的分母 | **已投票者过半**（谁投了算谁的分母）+ 参与人数门槛，详见 §7.1 | §7.1 / D-35 / D-36 |

**仍开放（定稿文档里也没有，编码前需拍板）**

| 编号 | 待定义内容 | 阻塞 | 说明 |
|---|---|---|---|
| 待定义-36 | 组长查询匿名提交者的**私聊口令名**（只知道是 P1） | M5 查询口 | 不阻塞 P0，可拖到 9/16 之后 |
| 待定义-37 | T01 原文"指令列表（5 条）"与路由表 **7 条**的**口径**：改述 T01，还是把后来加的两条并进 M0 的 5 条说法里 | 验收口径（T01 / M0） | 建议采"T01 指 M0 那 5 条"的读法（D-22），但需拍板。**不挡编码** |
| 待定义-42 | 飞书把「文字 + 附件」发成**一条**还是**两条**消息？（决定 `作业书` 指令怎么认文件） | M0 的**文件投递分支** | 2026-09-12 发现。测法已就位：`python -m tools.probe_feishu`。已实测**凭据有效 + 长连接可连**，收事件 / 下载文件待人在飞书里操作一次。**不阻塞 M0 其余部分**（前缀路由、登记表单都不依赖它）；应对见 D-42 |

**待定义-38 的备选方案（未采用，留档 —— 2026-09-11）**

- **群里一律 @机器人 才算指令**：能彻底消灭"闲聊里的数字被计票"。未采用的理由是要改 T05 原文"回复数字投票"，而 T05 属截图层真源。**若实测误判严重，随时启用**（这正是 §7.1 残留风险的备案）。
- **升级成交互卡片点选**：零误判，还能知道"这票是谁投的"。定稿已把它列为 **P1**，等 D5 门过了再说。
- **把投票搬到私聊**：会削弱"群里现场投票收敛"的演示效果（演示剧本第 3 步就是这个），不采用。

> **M4 的私聊志愿序号风险很低**：私聊里只有机器人和你，机器人上一句刚问过"你想做哪一块"，数字没有歧义。真正要动脑筋的只有 M2 的群内投票。

### 8.2 排期类

| 编号 | 原问题 | 结论 |
|---|---|---|
| 待定义-25 | **M4 / M5 / M6 没有 owner** | **已关闭**。定稿 §6 分工表补齐：**机器人 owner** = M0 + M1 + M4 / M5 指令路由 + **M6 定时任务**；**拆解 owner** = M2 + M3 + 评测脚本；**报告与评测 owner** = M7 + M8 + 演示材料。协作层与流程层归机器人 owner（D-28） |

> 分工是"今晚认领、可换"，但**必须有人认领**——9/16–9/17 是最挤的两天，不能空着。

### 8.3 背景类（不影响开发）

| 编号 | 问题 | 状态 |
|---|---|---|
| 待定义-20 | 「不整合」——被列为对话拍板决定之一，未解释含义 | **仍开放**：定稿文档全文亦无此词，出处只在那次对话里 |
| 待定义-21 | 「智能押拆解」——疑为笔误 | **已关闭**：是刻意措辞。定稿开篇"智能只押在拆解（M3）"，"押"= 把智能全押在一处 |
| 待定义-22 | 「研采」——队友代号还是方法论来源？ | **已关闭**：三个方案之一的**方案代号**。它贡献验收方法论——本文的 T01–T13 用例表与 D5 发布门即来自它 |
| 待定义-23 | 「赛道图」——用户最初提供的比赛要求 | **已关闭**（2026-09-12）：**不存在赛道图**。比赛交付物 = **运行视频 + 附件**；「附件」= 软件运行所需的一切，见 D-37 |
| 待定义-24 | 《方案一.docx》（意图层原件） | **仍开放**：位于 `C:\Users\617\Desktop\方案一.docx`，未纳入本仓库 |
| 待定义-26 | 「智汇简报」的 Dify 骨架 | **已关闭**：**工程兜底**。队友已有**可运行 Dify 原型**；主线彻底跑不通时用它保底交差（定稿 §7 备用路径 + 文末三方案定位） |

---

## 9. 决策台账

| 编号 | 决策 |
|---|---|
| D-01 | 需求真源 = 用户提供的**截图**；冲突时以此为准 |
| D-02 | 指令集见 §7.1（M0 原有 5 条，加上「完成 T3」与「登记」共 **7 条**）；**前缀精确匹配 + 状态优先，不做自然语言理解** |
| D-03 | 评分点结构见 §6.1；任务卡溯源字段 `rubric_refs` 见 §6.2。**定稿 §3.4 有明确出处**，不再是"后加设计"（原判断基于《方案一.docx》无数据模型，仍成立但已不是唯一依据）。落盘只存 `id` 数组、展示时 join 原文引用 |
| D-04 | 覆盖率分母用**人工标注**（防自出题自改卷）；80% 为初始参数 |
| D-05 | **全做 P0**。不可砍底线 = M0 / M1 / M3 / M7简版 / M8前5份；牺牲顺序 = M5 代言 → M2 方向候选 → M4 只保先到先得；工作量 8–10 人天 < 24 人天（三人并行） |
| D-06 | 机制继承自《方案一.docx》：志愿优先 / 不填随机 / 人人有活 → M4；学习通式临期提醒 → M6；智能分析报告 → M7；人为权重大于机器权重 → 组长指定改派（**降为 P1**） |
| D-07 | 飞书机器人即唯一 UI；WebSocket 长连接 |
| D-08 | **1 个 Agent + M0–M8 九个模块**，不是三 Agent。见 B1/B2/B3 |
| D-09 | 五层结构 + M8 旁挂，见 §2 |
| D-10 | **D5 发布门 = 9/15（周二）晚**。三条：① M0 测试群收发跑通；② 主链路命令行版跑通（M1+M3+M7简版）；③ 前 5 份覆盖率达标。**全过 → 开始 P1；任一不过 → 冻结全部 P1** |
| D-11 | **时间线**（9/11 周五 → 9/19 周六）：9/11 今晚定稿+认领分工 → 9/12–13 M0 网关骨架+M1 原型 → 9/14 M3 迭代+人工基线 → 9/15 **D5** → 9/16 指令层（M4+M5+M2）→ 9/17 交付层+全量评测 → 9/18 彩排 → 9/19 提交缓冲。**若官方截止非 9/19，整线以真实截止日为锚平移，D5 固定"截止前 4 天"** |
| D-12 | 数据存 JSON，不用数据库。**落盘位置统一为仓库根目录 `data/`**（`.gitignore`），`src/` 下不建存储目录（D-30） |
| D-13 | 存在命令行主链路，与飞书入口并行；M7 简版即命令行交付 |
| D-14 | 术语三分：评分点（M1）/ 任务卡（M3）/ 「模块」= `task_id` 的口语。**志愿对象 = `task_id`**；一张卡可横跨多章节，一个评分点可被多卡共覆盖 |
| D-15 | 作业书统一归一化为纯文本；拒收扫描版 PDF 与图片。详见 §7.5 |
| D-16 | M3 自检循环由代码判定；三层达标判定归属见 §7.2 / §7.3 |
| D-17 | 均衡度两套口径，禁止复用。详见 §7.4 |
| D-18 | 2 轮重拆仍不达标 → 输出最优 + 标注，由人拍板 |
| D-19 | **覆盖分母改为"可拆评分点"**（规格修正），详见 §7.2 / §7.3 |
| D-20 | `assignment.source` 扩为四值，详见 §6.4 |
| D-21 | 表格抽取策略 + 权重软校验，详见 §7.5 |
| D-22 | **指令集补第 6 条**：私聊「`完成 T3`」标记完成（定稿 §3.7）。T11 依赖它才可测。T01 截图"5 条"的口径见待定义-37 |
| D-23 | **截止时间是作业级、不是任务级**：M1 抽出 `deadline` 存 `data/assignment.json`（§6.0）；任务卡不设 `due_date`；M6 催办与 M7 甘特图共用这一根截止线 |
| D-24 | **任务卡 7 字段**（§6.2），含 `effort_hours` / `deliverable` / `acceptance`；`rubric_refs` 落盘存 id、展示拼原文 |
| D-25 | 待定义-31 / 32 / 33 / 35 采纳既定值（§7.3 / §7.5），编码不再阻塞 |
| D-26 | T01–T13 = **人工验收清单 + 执行记录表**（非自动化脚本）；方法论来自"研采" |
| D-27 | **M6 编号笔误勘误**：P1 的"M6 全量评测（15 份）"= **M8** 扩量。M8 用途 = 内部可信度来源 **兼** 比赛"可验收成果"硬通货 |
| D-28 | **模块 owner**：机器人 owner = M0 + M1 + M4/M5 指令路由 + M6；拆解 owner = M2 + M3 + 评测脚本；报告与评测 owner = M7 + M8 + 演示材料（关闭待定义-25） |
| D-29 | **方案层定稿文档归档**为 `docs/定稿文档-v2.md`（逐字转录），作为 §10 第 3 层依据 |
| D-30 | 落盘目录二义性（`src/data/` vs 根 `data/`）裁定：**唯一落盘位置 = 仓库根目录 `data/`**，见 `docs/ARCHITECTURE.md` §4 |
| D-31 | 「`完成 T3`」的落地字段 = `assignment.completed_at`（时间戳，`null` 表示未完成）；**MVP 不做提交物上传** |
| D-32 | **冲突裁决（用户 2026-09-11 拍板）**：截图 > 定稿文档 > 《方案一.docx》/ 对话拍板。第 2/3/4 层互撞时**一律听定稿**，不必再问人。见 §10 |
| D-33 | **路由优先级 = 先看状态、再看前缀**（补 B10）：`state.json` 的 `awaiting` 决定裸数字的解释（投票票号 / 志愿序号）；M2 投票窗口 = 过半 / 超时 10 分钟 / 组长拍板三者任一即关，一人一票、后投覆盖（用户 2026-09-11 拍板，采用候选 1「状态优先」）。详见 §7.1 |
| D-34 | **第 7 条指令「登记」+ 两步确认建花名册**（用户 2026-09-11 拍板）：组长在群里发「登记」→ 机器人回空白表单 → 组长 @人填回 → 机器人回显 → 组长回「同意」落盘 `data/members.json`。`open_id` 从 @ 结构里取，**免除"读群成员名单"权限**。详见 §6.6 / §7.7 |
| D-35 | **投票分母 = 已投票的人**（用户 2026-09-11 拍板）：某方向票数 > 已投票数 ÷ 2 即落定；配 D-33 的窗口。详见 §7.1 |
| D-36 | **工程默认（可推翻）**：投票加一道门槛——参与人数达**花名册人数的过半（向上取整）**才允许落定；超时且无人过半 → **交组长拍板**。防"第 1 票就宣布过半"（D-35 的退化情形） |

| D-37 | **比赛交付物 = 运行视频 + 附件；不存在赛道图**（用户 2026-09-12 拍板）。「附件」= 软件运行所需要的一切（源码、依赖、配置与运行说明）。三点含义：① 不存在未归档的评分表，"猜评分标准"的风险消除；② **运行视频是硬交付物** ⇒ 演示必须真跑、可录，9/18 彩排与演示路径升为关键路径；③ 真实作业书进群所需的**群文件下载权限是必需项**，不是可选项 |

| D-38 | **`check()` 返回失败原因列表，不是布尔**（工程实现口径，2026-09-12）：§7.3 的伪代码写 `return False` / `return eligible <= covered and ...`，但同节的流程要求"失败详情喂回重拆"，布尔带不了详情。落实现为 `list[str]`：空列表 = 通过，非空 = 喂回 LLM 的失败原因；`if not check(...)` 的用法与伪代码一致。空集短路与 0.5 地板照旧（待定义-31 不变）。见 `src/intelligence/decompose.py` |

| D-39 | **权重软校验"可归一化到百分制"的落地口径**（工程默认，可推翻，2026-09-12）：§7.5 只说"全部有 weight 且能归一化到百分制才执行"，但"加总 85（丢行）"与"20 分制"在数值上无法仅凭加总区分。实现取门限 `PERCENT_SCALE_MIN = 50`：加总 < 50 一律视为另一种量纲 → 整体跳过、不误报；≥ 50 才按 [90,110] 判是否疑似丢行。门限待真实作业书校准。见 `src/intelligence/extract.py` |

| D-40 | **模糊点建卡口径**（用户 2026-09-12 拍板）：`status="ambiguous"` 的评分点**允许建卡**（它对应的交付物可能真实存在），但卡的 `acceptance` 必须显式标注"该评分点为模糊要求，需组长/教师确认后方可核对"，**不得声称已满足**。模糊点**不进覆盖率的分子分母**（`coverage_loop()` 只收 `normal`），但它的卡**照常计入均衡计算**（`balance_loop()` 不筛 `rubric_refs`）。见 `src/intelligence/decompose.py` 的 `M3_SYSTEM` |

| D-41 | **真实作业书不入库**（2026-09-12）：定稿 §3.9 要收 15 份真实作业书构成评测集，但那是**别人课程的作业材料**（含版权与他人信息），放进交付物仓库不合适；且 M8 的可复现性靠的是**人工标注 + 独立脚本**，不是原文分发。因此：真实作业书只存本地、不 commit；入库的只有合成样本 `eval/samples/` 与标注 / 复算结果 |
| D-42 | **作业书文件怎么进机器人**（2026-09-12）：① **演示与验收走私聊投递** —— 私聊无需 @、机器人天然全收，先到的文件消息缓存住，随后的 `作业书` 文字指令与之配对；② **群投递作备选** —— 若实测证实飞书把「文字 + 附件」拆成两条、且 file 那条因无 @ 而收不到，则要么补"读群内全部消息"权限，要么改成"文件单独发 + 附言 @机器人"的两步约定。出处：§7.1 的实测说明 + `tools/probe_feishu.py`。**未定论部分**见待定义-42 |

---

## 10. 依据层级与出处

1. **截图层** —— 功能优先级表 + 验收用例表（`docs/requirements/`）。**需求真源，冲突时以此为准。**
2. **意图层** —— 《方案一.docx》：痛点、机制、定位，五段话，**无架构、无数据模型**。原件在 `C:\Users\617\Desktop\方案一.docx`，**未纳入仓库**（待定义-24）。
3. **方案层** —— 定稿文档：架构、模块、排期。**已归档为 `docs/定稿文档-v2.md`（逐字转录，D-29）**，是本仓库的**主要依据**（用户 2026-09-11 确认）。
4. **对话拍板** —— 不整合、志愿制、智能拆解、飞书机器人、人为权重大于机器权重。

**✅ 冲突裁决规则（已拍板，用户 2026-09-11 定）**

1. **第 1 层（截图）高于一切**——含定稿文档在内，谁跟截图冲突都以截图为准（D-01 不变）。
2. **第 2 / 3 / 4 层之间冲突 → 听第 3 层（定稿文档）**。即《方案一.docx》与"对话拍板"都让位于定稿文档（D-32）。
3. 理由：第 2 层原件不在仓库、无法逐句核对；第 4 层本文件自己就标着"二手转述"；只有第 3 层完整落盘、可逐句引用。

> 实操含义：以后发现《方案一.docx》或那次对话的说法跟定稿文档不一致，**不用停下问人，直接按定稿改，并在 D- 台账里记一笔**。

**⚠️ 断链警告** —— 第 4 类依据发生在另一个会话，本仓库无原始记录，只能记为**二手转述**；第 2 类原件在桌面上、未入库（待定义-24）。

**⚠️ 出处倒推提示** —— D-04（覆盖率用人工标注分母）属"后加设计"，《方案一.docx》无出处；D-03 里"落盘只存 id、展示拼原文"是工程决定，定稿只定义了 `rubric_refs` 字段本身。引用时必须说明来源。

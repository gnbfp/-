# AGENTS.md —— 本仓库的 AI 工作约定

## 0. 三句话看懂这个项目

- **是什么**：一个**飞书群机器人**，扮演小组里的"项目经理"。它的角色设定是 **"群里的第 N+1 个成员"**。
- **干什么**：小组在群里丢一份作业书 → 机器人抽出评分点 → 群里投票定方向 → 拆成任务卡 → 私聊收志愿并自动分配 → 执行期催办 → 输出执行报告。
- **形态**：**1 个 Agent + M0–M8 九个模块，单进程 Python 服务**，数据存 JSON 文件。

智能只押在**拆解（M3）**；聊天只是交互层，不是智能点。人只做两个动作：**私聊填志愿、群里投票**。机器人不提供思路、不替人写作业——只主持流程、匿名代言、发产物。

---

## 1. 新对话请按这个顺序读

1. **本文件** —— 读完就有全貌与当前状态
2. **`requirements.md`** —— 需求唯一真源（§1 边界 / §6 数据模型 / §7 机制 / §8 未定义清单 / §9 决策台账 / §10 依据层级）
3. **`docs/定稿文档-v2.md`** —— 方案层定稿：架构、模块、排期（主要依据）
4. **`docs/ARCHITECTURE.md`** —— 怎么实现；**§12 是"从零到能跑"的上手清单**
5. **`docs/GLOSSARY.md`** —— 名词速查（「方向」「模块」「花名册」「D5 门」……）
6. **`docs/requirements/`** —— 需求原件：两张截图 + 逐字转录（第 1 层真源，冲突时以它为准）

---

## 2. 文档地图

| 文件 | 管什么 |
|---|---|
| `AGENTS.md` | 本文件。AI 的入口与工作约定 |
| `requirements.md` | **需求唯一真源。动手前必须读。** 含 10 条硬性边界、场景、模块、数据模型、关键机制、未定义清单、决策台账 |
| `docs/定稿文档-v2.md` | 方案层定稿文档（逐字转录）。**主要依据**：架构、模块、排期。与截图冲突时仍以截图为准 |
| `docs/requirements/` | 需求原件：两张截图（功能优先级表 + 验收用例表）与逐字转录 |
| `docs/ARCHITECTURE.md` | 架构设计 v1.2。只讲"怎么实现"，不改需求 |
| `docs/GLOSSARY.md` | 名词速查表 |
| `tools/probe_feishu.py` | 飞书诊断探针：验凭据 / 长连接 / 文件投递与下载权限。**不属于 M0 运行时**，别被 `src/` import |

---

## 3. 铁律

**完整 10 条见 `requirements.md` §1（B1–B10）。最容易踩的六条：**

1. **不是三 Agent。** 不存在 Brain / Feishu / Data 的进程或目录划分。看到任何"子 Agent 之间通信"的设计，一律是错的。
2. **只有三处 LLM 调用**（M1 / M2 / M3）。M4–M7 是纯规则与模板，**零智能**——不要给它们加 LLM。
3. **不用数据库。** 数据存 JSON 文件，落盘只有仓库根目录 `data/` 一处。
4. **不写飞书文档。** 也不需要 `docx` 权限。
5. **LLM 不判定自己的产出。** 判定权在代码（M3 循环内）或独立脚本（M8）。
6. **拍板永远在人。** 机器兜底到建议为止。

---

## 4. 当前状态（2026-09-13）

**材料状态：定稿完成，`requirements.md` §8 里已无挡 P0 编码的未定义项 —— 可以开工。**

**代码进度（9/13）**：数据层 `config/models/storage` + 智能层 M1 抽取与解析、M3 判定与拆解循环 + `src/main.py` 命令行主链路已跑通；**M0 网关已并入 main（PR #4）**、**M8 评测集已落地**（`eval/`：基线 + 独立复算脚本，D-51）、**M4 志愿分配 + M5 匿名代言已落地**（`src/gateway/preference.py` / `allocation.py`，纯规则状态机、**零 LLM**；D-52~D-55）；247 个单测全绿（外审必修 1–6 已闭环），§11 五步人工验收部分已真机完成（指令路由、登记两步确认、作业书主链路均已实跑）；**M1 口径已两次收紧**（D-48 只抽评分标准小节、空 rubric 拒拆；D-49 `deadline` 放开非空 + 占位值软校验；D-50 `status` 判据改为"有没有具体交付物"），**M2、M6、M7 尚未开工**。**5 份真实作业书已在人工基线（左，2026-09-13）上复算**：逐份覆盖率 100%、均衡度 0.79–0.94，**D5 门③ 通过**（见 `eval/report.md`）。

- **已实测（2026-09-12）**：自建应用凭据有效（`tenant_access_token` 实测通过）、**WebSocket 长连接实测连得上**（`wss://msg-frontier.feishu.cn`），草稿机器人可运行。
- **指令集 = 7 条**（截图原文写的是 5 条）：`作业书` / `方向` / `拆解` / `你想做哪一块` / `我想提议：…` / `完成 T3` / `登记`。
- **时间线**：9/11 定稿 + 认领分工 → 9/12–13 M0 网关 + M1 原型 → 9/14 M3 + 人工基线 → **9/15 D5 发布门** → 9/16 指令层 → 9/17 交付层 + 全量评测 → 9/18 彩排 → 9/19 提交。
- **D5 发布门（9/15 晚，三条全过才做 P1）**：① 测试群收发跑通；② 主链路命令行跑通（作业书 → 任务卡 → 核对清单）；③ 前 5 份覆盖率达标。任一不过 → **冻结全部 P1**。**D5 发布门三条（9/15）现已提前全绿**（9/13：① 群收发 ② 命令行主链路 ③ 覆盖率 5/5），9/15 复查一次即可放行 P1。
- **仍开放（都不挡编码）**：`待定义-36`（组长查询的口令名，P1 可拖）、`待定义-37`（T01 那句"5 条"怎么改述）；背景三项见 §8.3。
- **待办（2026-09-13）**：① ~~补飞书权限~~ **已完成** —— 权限已开齐，实测 6/6 事件全收到、文件可下载（见 §7.1）；② ~~真实作业书~~ **5 份已收齐**（BIM / 数据库 / 软件系统设计 / 营销方案 / 河流健康调查），人工基线（左）+ M8 复算已跑通（`eval/report.md`；**原文不入库**，见 D-41）；③ 认领三个 owner（机器人 / 拆解 / 报告与评测）—— **报告与评测 = 左（已认领）**；④ ~~CJK 部首归一化~~ **已实现**（**D-43**）：`normalize_cjk()` 在抽取唯一出口调用，真实 PDF 实测残留部首 176→0、总长不变；另加 `check_radical_residue()` 兜底（漏网部首在核对清单里以 `[软警告]` 点名），7 份真实作业书实测残留 0；⑤ ~~M8 人工基线~~ **已完成**（2026-09-13）：左（代号）独立重标 5 份 baseline（`annotator=左`）。盲标依据：左全程未参与本项目开发，仅提供作业书原文，未接触任何 agent 产物、未看过群内核对清单。复算 5/5 通过（逐份 100%、无 ⚠️），**门③ 正式生效**。
- **一条工程默认可随时推翻**：`D-36`（投票参与人数门槛）。

---

## 5. 工作方式

1. **动手前先读完 `requirements.md`。**
2. **遇到 `requirements.md` §8 里仍开放的条目 → 停下问人，禁止臆想。** 这是本项目的硬规矩；没定义的东西没有"合理默认值"这一说。
3. **看到材料之间打架 → 按 `requirements.md` §10 裁决：截图 > 定稿文档（`docs/定稿文档-v2.md`）> 《方案一.docx》/ 旧对话。** 第 2/3/4 层互撞时直接按定稿改，不用停下问人，改完在 §9 决策台账记一笔。
4. **判定权不在 LLM 手上。** LLM 只生成，判定由代码（M3 循环内）或独立脚本（M8）执行。
5. **拍板永远在人。** 机器兜底到建议为止。
6. 改动接口或数据模型 → 同步更新 `requirements.md` 的决策台账，并通知全员。

---

## 6. Git 约定

- **单仓库（monorepo）**，不拆模块仓库。
- `main` 受保护：Require pull request before merging，Required approvals = **1**。
- 分支命名：`feat/<模块>-<内容>`、`fix/<内容>`。
- **Tag 只在 `main` 上打，且必须 push**：`git tag v0.2-xxx && git push origin v0.2-xxx`。只打本地 Tag 等于没打。
- 第一天就先 push 一个空 README，验证 GitHub 连通性（国内访问不稳定）。
- 不配 CI（8 天周期不值得），但**PR 记录要留**——交付物含源码，提交历史是分工的证据。
